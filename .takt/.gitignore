# Ignore everything by default
*

# This file itself
!.gitignore

# Project configuration
!config.yaml

# workflows/, steps/, facets/ are generated by scripts/use-lang.sh from
# the language directories (en/, ja/) at the repository root and are not tracked.
